const footer = `flex flex-wrap items-center justify-center gap-2 p-4 mx-auto w-fit text-white`;

const text = `text-base text-off-white`;

const link1 = `text-sm text-light-blue hover:text-green`;

const link2 = `text-sm text-light-blue hover:text-orange`;

const link3 = `text-sm text-light-blue hover:text-blue`;

const style = {
  footer,
  text,
  link1,
  link2,
  link3
};

export default style;